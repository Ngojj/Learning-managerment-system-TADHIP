
  # User Registration System

  This is a code bundle for User Registration System. The original project is available at https://www.figma.com/design/U8PudDjgj44TLPedSiUWBe/User-Registration-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  